# TM32_HAL_Modules
基于 STM32 HAL 库的通用硬件复用模块库，封装 LED、按键、串口等常用外设，低耦合、易移植，可快速集成到各类 STM32 项目。
Einglish:
  A reusable hardware module library for STM32 HAL, encapsulating common peripherals (LED, key, UART, etc.) with low coupling and easy portability.
